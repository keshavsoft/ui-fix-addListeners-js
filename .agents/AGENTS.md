# Rules

## Git Rules
- **NEVER** run git commits or git pushes (`git commit`, `git push`, etc.) without the user's explicit permission. Always show the modified files and ask for permission before running any commit or push command.
